// Shared marker for products submitted by the KELIKULI Custom Lab
// (custom.kelikuli.com) integration. Used by both the intake API route
// and the admin products list so drafts from that source are identifiable.
export const CUSTOM_LAB_TAG = "Custom Lab";
